import * as React from 'react';

interface Props {}
const Example: React.FC<Props> = (props): JSX.Element => {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
};

export default Example;
